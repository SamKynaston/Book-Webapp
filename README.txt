Backend Port: 3000
Frontend Port: 5173

Instead of localhost, you must get your system's local IP address and use that instead. This was done to make it easier for external devices to test the system. 

You must also have docker installed and updated: ./init.sh will do the rest. If it fails to execute, run chmod +x ./init.sh.