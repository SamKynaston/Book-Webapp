export enum InventoryStatus {
    AVAILABLE = "AVAILABLE",
    BORROWED = "BORROWED",
    MISSING = "MISSING"
}

export enum BorrowStatus {
    ACTIVE = "ACTIVE",
    RETURNED = "RETURNED",
    OVERDUE = "OVERDUE"
}